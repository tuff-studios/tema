# why not paper-plugin.yml?
name: TEMA
version: '${version}'
main: dev.craftwarestudios.tema.ExamplePlugin
author: xw1w1

# the fuck is this
commands:
  tema-debug:
    description: Open the GUI example
    usage: /tema-debug [shared|instance]
permissions:
  gui.example:
    default: true
